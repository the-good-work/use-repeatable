import React from "react";
export declare function ClearButton({ onClick, children, removeAll, className, }: {
    onClick?: () => void;
    children: React.ReactNode;
    removeAll: () => void;
    className?: string;
}): JSX.Element;
